# Base URL for the market texture API host (no trailing slash)
MARKET_TEXTURE_API_BASE_URL=https://your-market-texture-api-host.example.com

# Bearer token for /market/texture
MARKET_TEXTURE_API_TOKEN=replace-with-your-token

# Optional API key header (x-api-key)
MARKET_TEXTURE_API_KEY=
